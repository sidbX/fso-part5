const Notification = ({ message }) => (
    message!=='' &&
  <>
    <div style={{ color: 'black', border: '5px solid black', fontSize: 30, background: 'grey'  }}>{message}</div>
  </>
)

export default Notification
